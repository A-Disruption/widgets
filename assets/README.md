# Embedded assets

`lucide.ttf` is copied from `lucide-static` 1.27.0. The `menu` widget uses it
for the checkmark on a `toggle` row and the chevron on a `submenu` row. The
upstream license is preserved in `LUCIDE-LICENSE`.

Source: https://github.com/lucide-icons/lucide
